#include <stdio.h>
#include <stdlib.h>


int main(void) {
	char str[50];
    printf("enter string: ");
    gets(str);//karan is a good boy(enter this string to see and your prob is solved)
    puts(str); 
	return 0;
}