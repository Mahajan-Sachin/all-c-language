#include<stdio.h>
int main(){
    int x=5,y=3,z;
    printf("%d",x--);//here x-- is a post no change in value but after printing it will change
    // z=x-- -y;//here x-- is a post 
    // printf("the value is: %f",v);
    return 0;
}