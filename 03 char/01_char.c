#include<stdio.h>

int main() {
    char x = '1';  // Use single quotes for character literals
    char y = 'k';  // Use single word/alphabet/symbol for character literals
//when you are saying you are taking a char then never ever write strings like 'karan'
    printf("%c", x);
    printf("\n%c", y);

    return 0;
}
